# ============================================================
# ANA - Agentic Network Assistant
# Environment Configuration
# ============================================================
# Copy this file to .env and adjust values for your environment.
#   cp .env.example .env       (Linux/macOS)
#   copy .env.example .env     (Windows)
# ============================================================

# ── Port Configuration ──────────────────────────────
# Ports are auto-detected by setup.py if defaults are busy.
# Set explicitly to override auto-detection.
# ANA_FRONTEND_PORT=8501
# ANA_BACKEND_PORT=9000

# ── Backend API ───────────────────────────────────────
# Base URL for the FastAPI backend (auto-set by setup.py)
API_BASE=http://localhost:9000

# ── AI / LLM Configuration (Optional) ────────────────
# Google Gemini API key for AI-powered assistant features
# Get a free key at: https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Mistral / Ollama offline fallback endpoint
# Install Ollama: https://ollama.com → ollama pull mistral
MISTRAL_API_URL=http://localhost:11434/api/generate
